@ node7
1. Clone nus-sys/capybara and nus-sys/capybara-redis side by side
2. in capybara-redis
    $ make -j BUILD_TLS=yes
3. in capybara
    $ git checkout dev-cowsay
    $ ./scripts/setup/dpdk.sh
    $ make init
    $ make LIBOS=catnip CAPY_LOG=1 TCP_MIG=1 all-libs
    $ make LIBOS=catnip CAPY_LOG=1 TCP_MIG=1 all-examples
    $ ./scripts/rsync.sh

@ node8
1. in capybara
    $ make fe-dpdk-ctrl
2. with new shell in capybara
    $ make redis-server-node8

@ node7
1. in capybara-redis
    $ ./src/redis-cli --tls --cert /usr/local/tls/svr.crt --key /usr/local/tls/svr.key --cacert /usr/local/tls/CA.pem -h 10.0.1.8 -p 10000